<%= form_for(@deweys_power_panel) do |f| %>
  <% if @deweys_power_panel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deweys_power_panel.errors.count, "error") %> prohibited this deweys_power_panel from being saved:</h2>

      <ul>
      <% @deweys_power_panel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
