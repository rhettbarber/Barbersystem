


<%  @items = Item.find(:all, :conditions => ["category_id = ?", category.id ] ) %>

<% for item in @items %>

<%=      render(:partial => "item",:locals => { :item => item}, :layout => 'items'  )  %>

<% end %>