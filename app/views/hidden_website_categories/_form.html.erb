<%= form_for(@hidden_website_category) do |f| %>
  <% if @hidden_website_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hidden_website_category.errors.count, "error") %> prohibited this hidden_website_category from being saved:</h2>

      <ul>
      <% @hidden_website_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :website_id %><br />
    <%= f.text_field :website_id %>
  </div>

  <div class="field">
    <%= f.label :category_id %><br />
    <%= f.text_field :category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
