 <div id="back_choices" style="float: left;display:none;" class="x_choices">
                    <%= form_for( 'back_javascript',  :url => {  :action => 'update_categories'}, :html => { :id =>  "back_form" } ) do |f| %>
                                    <%= f.collection_select :department_id,  @department_choices_back , :id, :Name, :prompt => "Select a department" %>
                                    <span id="back_category_field">
                                          <%= f.collection_select :category_id, Category.limit("2"), :id, :Name, :prompt => "Select a category" %>
                                    </span>
                                    <span id="specsheet_item_search_back_field_container">
                                          <%= text_field_tag "specsheet_item_search_back_field" %>
                                          <%= image_tag '/images/icons/search-icon.png', :size => '40x40' %>
                                    </span>
                    <% end  %>
                    <div id="category_items_back">
                                 <%= render :partial => "designer_specsheet/category_items_back"  %>
                    </div>
 </div>
